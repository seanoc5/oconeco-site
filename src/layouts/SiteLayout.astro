---
interface Props {
  title: string;
  description?: string;
  mainClass?: string;
  isArticle?: boolean;
}
const { title, description, mainClass = "", isArticle = false } = Astro.props;
const pageTitle = title === "Oconeco" ? "OconEco" : `${title} — OconEco`;

const cfBeaconToken = import.meta.env.PUBLIC_CF_BEACON_TOKEN;
const enableAnalytics = import.meta.env.PROD && cfBeaconToken;

const SITE_URL = "https://oconeco.com";
const ORG_ID = `${SITE_URL}/#organization`;
const WEBSITE_ID = `${SITE_URL}/#website`;
const ORG_DESCRIPTION =
  "OconEco publishes the FAND national balance sheets: 237 countries today, with growing subnational coverage, in an accounting framework that complements GDP.";

const pagePath = Astro.url.pathname;
const canonicalUrl = new URL(pagePath, SITE_URL).toString();

const orgJsonLd = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "@id": ORG_ID,
  name: "OconEco",
  url: SITE_URL,
  description: ORG_DESCRIPTION,
  sameAs: [],
};

const websiteJsonLd = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "@id": WEBSITE_ID,
  url: SITE_URL,
  name: "OconEco",
  inLanguage: "en",
  publisher: { "@id": ORG_ID },
};

function humanize(segment: string): string {
  const spaced = segment.replace(/-/g, " ");
  return spaced.charAt(0).toUpperCase() + spaced.slice(1);
}

const trimmedPath = pagePath.replace(/^\/|\/$/g, "");
const segments = trimmedPath ? trimmedPath.split("/") : [];
const breadcrumbItems = [
  {
    "@type": "ListItem",
    position: 1,
    name: "Home",
    item: `${SITE_URL}/`,
  },
  ...segments.map((seg, i) => ({
    "@type": "ListItem",
    position: i + 2,
    name: humanize(seg),
    item: `${SITE_URL}/${segments.slice(0, i + 1).join("/")}/`,
  })),
];

const breadcrumbJsonLd = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: breadcrumbItems,
};

const articleJsonLd = isArticle
  ? {
      "@context": "https://schema.org",
      "@type": "Article",
      headline: title,
      ...(description ? { description } : {}),
      url: canonicalUrl,
      mainEntityOfPage: canonicalUrl,
      inLanguage: "en",
      publisher: { "@id": ORG_ID },
      isPartOf: { "@id": WEBSITE_ID },
    }
  : null;

const nav = [
  { href: "/", label: "Home" },
  { href: "/evidence/", label: "The Data" },
  { href: "/framework/", label: "What We Measure" },
  { href: "/methodology/", label: "Methods" },
  { href: "/about/", label: "About" },
];

const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
function isCurrent(href: string): boolean {
  const normalized = href.replace(/\/$/, "") || "/";
  if (normalized === "/") return currentPath === "/";
  return currentPath === normalized || currentPath.startsWith(normalized + "/");
}

const ogType = isArticle ? "article" : "website";
const ogImage = new URL("/og-default.png", Astro.site).href;
const ogUrl = Astro.url.href;
const ogDescription = description ?? ORG_DESCRIPTION;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    {description && <meta name="description" content={description} />}
    <title>{pageTitle}</title>
    <meta property="og:type" content={ogType} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={ogDescription} />
    <meta property="og:url" content={ogUrl} />
    <meta property="og:site_name" content="OconEco" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={ogDescription} />
    <meta name="twitter:image" content={ogImage} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,600;1,8..60,400&display=swap"
    />
    <link rel="stylesheet" href="/styles/site.css?v=2" />
    <link rel="alternate" type="application/rss+xml" title="OconEco" href="/rss.xml" />
    <script type="application/ld+json" set:html={JSON.stringify(orgJsonLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteJsonLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbJsonLd)} />
    {articleJsonLd && (
      <script type="application/ld+json" set:html={JSON.stringify(articleJsonLd)} />
    )}
    {enableAnalytics && (
      <script
        defer
        src="https://static.cloudflareinsights.com/beacon.min.js"
        data-cf-beacon={`{"token": "${cfBeaconToken}"}`}
      />
    )}
  </head>
  <body>
    <header class="site-header">
      <div class="site-header-inner">
        <a class="brand" href="/" aria-label="OconEco — home">
          <img class="brand-logo" src="/OconEco.logo.png" alt="OconEco" width="1357" height="402" />
        </a>
        <nav class="site-nav" aria-label="Primary">
          <ul>
            {nav.map((item) => (
              <li>
                <a
                  href={item.href}
                  aria-current={isCurrent(item.href) ? "page" : undefined}
                >
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </div>
    </header>
    <slot name="hero" />
    <main class={`site-main ${mainClass}`}>
      <slot />
    </main>
    <footer class="site-footer">
      <div class="site-footer-inner">
        <p>
          <img class="footer-logo" src="/OconEco.logo.png" alt="" width="1357" height="402" />
          &copy; {new Date().getFullYear()} OconEco. All rights reserved.
        </p>
        <p>
          <a href="/site-map/">Site map</a>
          &nbsp;·&nbsp;
          <a href="/contact/">Contact</a>
        </p>
      </div>
    </footer>
  </body>
</html>
