# raw/

Unprocessed dumps — email exports, meeting transcripts, draft notes.

**Private and unedited.** Read for context but don't quote verbatim on the site.

When something in here turns out to be load-bearing, distill it up into `vision/` or `theory/` so future agents don't have to re-derive it from the raw source.
